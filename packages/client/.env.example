# API base URL (without trailing slash)
# Leave empty to use same origin (Vite dev server proxies /api automatically)
VITE_API_URL=http://localhost:3000

# WebSocket URL
# Leave empty to connect to same origin
VITE_WS_URL=
